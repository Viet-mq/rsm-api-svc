package com.edso.resume.api.domain.rabbitmq;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class BaseRabbitAction {
    protected final Logger logger = LoggerFactory.getLogger(this.getClass());
}
